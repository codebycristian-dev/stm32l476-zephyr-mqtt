# ESP32-C6 ESP-AT preparation manifest

Authorization status: EXECUTED ONCE on 2026-08-26 after the recorded final
gate. The complete, ignored execution log is
`private/espat-evidence/provision-20260826T-authorized.log`. A later erase or
write remains destructive and requires fresh authorization bound to its own
then-current identity, backup digest, firmware hash, artifacts, offsets, and
command.

## Target and strategy

- USB bridge: WCH `1a86:55d3`, serial `5B14063285`; stable by-id path resolved fresh
- Chip: ESP32-C6 revision 0.2 (must be freshly reconfirmed)
- Physical flash: 16 MB / `0x1000000` bytes (must be freshly reconfirmed)
- Strategy: custom local build; official release compatibility is not established
- ESP-AT: tag `v4.1.1.0`, commit `7c092f9aee793d6dbfae31e7585637baa14fb4ce`
- ESP-IDF requirement (`module_config/module_esp32c6-16mb-tcp/IDF_VERSION`): repository `https://github.com/espressif/esp-idf.git`, branch `release/v5.4`, exact commit `8ad0d3d8f2faab752635bee36070313c47c07a13`
- ESP-IDF actually built: detached exact commit `8ad0d3d8f2faab752635bee36070313c47c07a13` (`git describe`: `v5.4.1-643-g8ad0d3d8f2f`)
- Source: `https://github.com/espressif/esp-at`, recursively checked out under ignored `private/espat-source/`

Espressif's release matrix labels the available C6 artifact
`ESP32-C6-4MB-AT-V4.1.1.0.zip`, specifies 4 MB flash, and supports only the
listed MINI-1/WROOM-01 hardware by default. A detected 16 MB capacity does not
prove that image or its partition layout is compatible with this board.

## Required custom configuration

- Target: ESP32-C6, physical/declared flash size 16 MB
- Flash: DIO, 80 MHz, 16 MB
- AT port: UART1; GPIO6 RX, GPIO7 TX; 115200 baud; 8N1; no RTS/CTS
- Scope: base, Wi-Fi, and TCP/IP commands; MQTT is disabled/not depended upon
- UART framing/flow: 115200, 8 data bits, no parity, 1 stop bit, no hardware flow control; factory CTS/RTS values are `-1`
- Partition layout: `firmware/espat-16mb/partitions_at.csv`; OTA metadata `0xd000..0xf000`, PHY `0xf000..0x10000`, NVS `0x10000..0x1e000`, AT customization `0x1e000..0x60000`, OTA0 `0x60000..0x830000`, OTA1 `0x830000..0x1000000`; ranges are non-overlapping and bounded by 16 MB
- Custom configuration name: `ESP32C6-16MB-TCP`
- Build commands: `scripts/prepare-espat-source.sh`, then from the pinned source with the ESP-IDF-managed interpreter, `/home/cristian/.espressif/python_env/idf5.4_py3.14_env/bin/python ./build.py build`
- Build result: success using ESP-AT's normal `build.py` workflow; compiler `riscv32-esp-elf GCC 14.2.0`, esptool `4.12.0`

## Generated build outputs

Ignored build root: `private/espat-source/esp-at-v4.1.1.0/build/`.

| Offset | Generated binary | Bytes | SHA-256 |
|---:|---|---:|---|
| `0x000000` | `bootloader/bootloader.bin` | 22,128 | `fa70ff166c9dd078e497de11ec8394f166cda2727ca78b52d1fa67c74c7f642c` |
| `0x008000` | `partition_table/partition-table.bin` | 3,072 | `c52ff50ca4daef5d3bfb0289bfffffba0aac75f738e76f44ec8c4e144547a789` |
| `0x00d000` | `ota_data_initial.bin` | 8,192 | `7d2c7ac4888bfd75cd5f56e8d61f69595121183afc81556c876732fd3782c62f` |
| `0x01e000` | `at_customize.bin` | 3,072 | `f5a36f571968b2b6afc21872dfc4e006b58c8eea2d469c8c5af7b60ef2fcc01f` |
| `0x01f000` | `customized_partitions/mfg_nvs.bin` | 126,976 | `ae00655ebedfa9dc5f85d2d1ce990e5d04dc1b0931f704694dc6bfe4a432ba37` |
| `0x060000` | `esp-at.bin` | 1,155,008 | `572476ca8076161a55f7445bc88bd14a75e1e1fdbd89ef26974d98d73ee012a5` |

Combined images:

- Preferred first-provisioning image: `factory/factory_ESP32C6-16MB-TCP.bin`, exactly 16,777,216 bytes, SHA-256 `46fe184cc79dac002e55c278be878a7b863744dbdaf4691f2f4bea351a84e806`. It is generated by esptool `merge_bin --fill-flash-size 16MB` from the six binaries above and is valid at offset `0x0`.
- Reproducible unfilled combined image: `factory/factory_ESP32C6-16MB-TCP_unfilled.bin`, 1,548,224 bytes, SHA-256 `31b65e583bfbd5792dfd7d4b5e7f11b3b2e4447619d8323babcfc97bd7be0d6e`.
- Required metadata exists: `download.config`, `flasher_args.json`, generated root `sdkconfig`, and `module_info.json`.

Generated `download.config` (verbatim):

```text
--flash_mode dio --flash_freq 80m --flash_size 16MB 0x0 bootloader/bootloader.bin 0x60000 esp-at.bin 0x8000 partition_table/partition-table.bin 0xd000 ota_data_initial.bin 0x1e000 at_customize.bin 0x1f000 customized_partitions/mfg_nvs.bin
```

Generated configuration validation:

- `sdkconfig`: target `esp32c6`; DIO; 80 MHz; 16 MB; custom table at `0x8000`. The enabled AT command groups are exactly base, UART, Wi-Fi, and network/TCP; MQTT, HTTP, OTA, mDNS, ping, signaling, SmartConfig, WPS, user commands, Bluetooth, BLE, and BluFi AT support are not enabled.
- Generated `mfg_nvs.csv`: module `ESP32C6-16MB-TCP`, UART port `1`, 115200 baud, TX `7`, RX `6`, CTS `-1`, RTS `-1`.
- Partition table: bootloader region ends before `0x8000`; table occupies `0x8000..0x8c00`; OTA data `0xd000..0xf000`; PHY `0xf000..0x10000`; NVS `0x10000..0x1e000`; AT customization `0x1e000..0x60000`; OTA0 `0x60000..0x830000`; OTA1 `0x830000..0x1000000`. All required alignments pass, ranges do not overlap, and all ranges fit within 16 MiB.
- Each OTA application slot is exactly `0x7d0000` = 8,192,000 bytes (ESP-IDF's decoder displays this as `8000K`, meaning 8000 KiB). `esp-at.bin` is 1,155,008 bytes, leaving exactly 7,036,992 bytes (`0x6b6040`) in either application slot.
- `scripts/validate-espat-build.py private/espat-source/esp-at-v4.1.1.0` independently validates the generated map, ranges, alignment, feature boundary, UART data, factory image, sizes, and hashes.

Exact generated partition table (end is exclusive):

| Name | Type/subtype | Offset | Size (hex) | Size (bytes) | End |
|---|---|---:|---:|---:|---:|
| `otadata` | `data/ota` | `0x00d000` | `0x002000` | 8,192 | `0x00f000` |
| `phy_init` | `data/phy` | `0x00f000` | `0x001000` | 4,096 | `0x010000` |
| `nvs` | `data/nvs` | `0x010000` | `0x00e000` | 57,344 | `0x01e000` |
| `at_customize` | `0x40/0` | `0x01e000` | `0x042000` | 270,336 | `0x060000` |
| `ota_0` | `app/ota_0` | `0x060000` | `0x7d0000` | 8,192,000 | `0x830000` |
| `ota_1` | `app/ota_1` | `0x830000` | `0x7d0000` | 8,192,000 | `0x1000000` |

`ota_data_initial.bin` is exactly 8,192 bytes of `0xFF`. With no factory app partition, the pinned ESP-IDF bootloader treats this as initial OTA metadata, selects OTA0, and initializes its sequence record. The generated flash map places the valid `esp-at.bin` image at OTA0's `0x60000` start.

Provisioning-method write scope:

- Filled factory image: writes the entire half-open range `0x000000..0x1000000`, including `0xFF` in every gap, all data partitions not otherwise populated, unused OTA0 bytes, and all of OTA1. This produces a fully deterministic 16 MiB flash state.
- Generated multi-bin command writes only: bootloader `0x000000..0x005670`; partition-table binary `0x008000..0x008c00`; initial OTA metadata `0x00d000..0x00f000`; AT customization header `0x01e000..0x01ec00`; manufacturing NVS `0x01f000..0x03e000`; and ESP-AT application `0x060000..0x179fc0`. Every other byte retains its previous flash contents.

For first provisioning, the filled 16 MiB factory image is preferred: its generated composition was reproduced byte-for-byte, it initializes every required input at the reviewed offsets, and it removes dependence on stale contents in gaps, NVS, unused OTA0 space, or OTA1. Recovery for either method remains a separately authorized full write of the verified 16 MiB backup; neither method weakens or replaces that backup.

## Backup evidence

- Convention: `private/espat-backups/esp32c6-<UTC>-full-16MiB.bin`
- Required read: `python -m esptool --chip esp32c6 --port <matched-by-id> read-flash 0x000000 0x1000000 <backup>`
- Path: `private/espat-backups/esp32c6-20260815T035358Z-full-16MiB.bin`
- Size: 16,777,216 bytes
- SHA-256: `26ab342b257fe9ab5146beaa61a5b7d277457cdf49f1a899d7cdb799b22e2695`
- esptool: 5.3.1

## Authorized destructive workflow — executed once

The final gate revalidated the WCH `1a86:55d3` serial `5B14063285`, ESP32-C6
revision v0.2, 16 MB detected flash, the preserved backup digest
`26ab342b257fe9ab5146beaa61a5b7d277457cdf49f1a899d7cdb799b22e2695`, and the
filled factory image digest
`46fe184cc79dac002e55c278be878a7b863744dbdaf4691f2f4bea351a84e806`. It then
executed exactly the factory-image command below once. Esptool reported
`Wrote 16777216 bytes`, verified the written hash, and hard-reset the device.
The full command and output are retained in the execution log named above.

Executed factory-bin command:

```text
python -m esptool --chip esp32c6 --port <freshly-matched-by-id> --baud 460800 --before default-reset --after hard-reset write-flash --flash-mode dio --flash-freq 80m --flash-size 16MB 0x0 build/factory/factory_ESP32C6-16MB-TCP.bin
```

Alternative multi-bin command generated from `flasher_args.json`/`download.config`
(not executed):

```text
python -m esptool --chip esp32c6 --port <freshly-matched-by-id> --baud 460800 --before default-reset --after hard-reset write-flash --flash-mode dio --flash-freq 80m --flash-size 16MB 0x0 build/bootloader/bootloader.bin 0x8000 build/partition_table/partition-table.bin 0xd000 build/ota_data_initial.bin 0x1e000 build/at_customize.bin 0x1f000 build/customized_partitions/mfg_nvs.bin 0x60000 build/esp-at.bin
```

## Credential-free post-provision verification

The post-reset UART0 capture is retained at
`private/espat-evidence/uart0-postflash-20260826T-bounded.raw`. It directly
reports DIO, 80 MHz, `SPI Flash Size : 16MB`, the expected 16 MiB partition
table ending at `0x1000000`, and UART1 `tx:7 rx:6 cts:-1 rts:-1 baudrate:115200`.

The independent NUCLEO-L476RG diagnostic record retained by
`change/qualify-espat-development-modem` then completed `AT` once (TX 4, RX
11, final `OK`) and `AT+GMR` once (TX 8, RX 198, final `OK`). Timeout, TX
error, overflow, truncation, UART parity/framing/noise/overrun, and ring
overflow counters were all zero; no MPU fault, stack overflow, or Zephyr fatal
error occurred. It exercised neither Wi-Fi credentials, TCP, MQTT, nor a
second modem mutation.

`AT+GMR` identified the installed image as ESP-AT `4.1.1.0` (`ba4dd0e`,
ESP32C6, Jul 31 2025 08:37:48), ESP-IDF
`v5.4.1-643-g8ad0d3d8f2f-dirty`, compiled Aug 26 2026 05:59:24.

### `ESP32C6-4MB` runtime label

`AT+GMR` reported `Bin version:v4.1.1.0(ESP32C6-4MB)`, while the generated
build metadata is explicitly 16 MB: `sdkconfig`, `download.config`, and
`flasher_args.json` specify 16 MB; the factory image is 16,777,216 bytes; and
the observed boot partition table ends at `0x1000000`. ESP-AT renders the
parenthetical bin label from its factory module-name data, not from an
esptool flash probe. The generated factory-NVS CSV records
`ESP32C6-16MB-TCP`, whereas the observed runtime label remained `ESP32C6-4MB`.
That metadata discrepancy is retained as a provenance follow-up; it is not
used to infer physical capacity and does not invalidate the direct 16 MB
bootloader, partition-table, and write evidence. No firmware was modified
merely to rename the label.

Recovery would require a separately authorized full write of the verified
16 MiB backup and is not risk-free.
